#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#define ELEMENT 100

int main(void)
{
  int num[ELEMENT] = {0};
  int i = 0;

  for(i = 0;i < ELEMENT;i++){
    num[i] = i;
  }

  int S_num[ELEMENT] = {0};

  for(i = ELEMENT;i >= 0;i--){
    int temp;
    temp = rand() % i;